spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/diplom
    username: admin_diplom
    password: 123456
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
    contexts: dev
    change-log: 
      classpath:db/changelog/db.changelog-master.xml
  mvc:
    static-path-pattern: /resources/**